#!/bin/bash
<<====================================================
Subprocess

When a shell script is launched, a subprocess is created.

A subprocess is a child process of a launching process.

A shell script is launched by the bash command shell.

A subprocess can itself launch a subprocess.
When a shell script is launched, a subprocess is created.

A subprocess is a child process of the Subprocess
 

====================================================
