Encountered most often.

Sequence of words separated by blanks, terminated by a control operator.

Typically, the first word is the command, with subsequent words being command arguments.

Commands return an exit status.

Non-zero exit statuses indicate command failure.
